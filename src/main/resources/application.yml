spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3305/search?autoReconnect=true&allowMultiQueries=true&sessionVariables=wait_timeout=600&zeroDateTimeBehavior=convertToNull

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
